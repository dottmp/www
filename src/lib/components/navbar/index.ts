import ArticleNavbar, { type ArticleNavbarProps } from './article-navbar.svelte';
import MainNavbar from './main-navbar.svelte';
import Root, { type NavbarProps } from './navbar.svelte';

export {
	Root,
	type NavbarProps as Props,
	MainNavbar as Main,
	type ArticleNavbarProps,
	ArticleNavbar as Article
};
